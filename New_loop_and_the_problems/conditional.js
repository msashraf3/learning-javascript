var iphonePrice = 79000;
var myBudget = 9500;
//this will not execute,cuz the condition is false
if(iphonePrice < myBudget){
  console.log(`iphone kinsi re mama`);
}

/*** 
 * if iPhone price is less than my budget.
 * i will buy the iphone
 * if(condition){
 *   inside this will execute if the condition is true
 * }
 *
 */

var iphonePrice = 79000;
var myBudget = 95000;


if(iphonePrice < myBudget){
  console.log(`iphone kinsi re mama`);
}

var chickenPrice = 500;
var myMoney = 50;

if (chickenPrice<=myMoney) {
   console.log(`murgi raan khabo ar haddi chibabo`);
}
if (chickenPrice > myMoney) {
   console.log(`Go for smashed potato & lentil soup`);
}
// Without writing this way you can write it in different way 

/**
 * if i have enough money i will buy chicken. otherwise or else i will get smatched potato & lentil soup.
 */

if (chickenPrice < myMoney) {
   console.log(`i will eat the chicken grill`);
} else {
   console.log(`go for smashed potatoes you broke`);
}
/**
 * one condition has to be true otherwise else will execute. else don't need any condition 
 */
